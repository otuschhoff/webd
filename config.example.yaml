# Route evaluation is prefix-based and the longest matching path wins.
# Keep more specific routes (like /api/) alongside a final catch-all / route.
routes:
  # Requests beginning with /api/ are proxied to backend API v1 paths.
  - path: /api/
    handler: http://127.0.0.1:8080/api/v1/
    # Optional IPv4 client allow-list for this route. Supports:
    # - single IPv4: 192.0.2.10
    # - range: 192.0.2.10-192.0.2.50
    # - subnet (CIDR): 192.0.2.0/24
    allowed_ipv4:
      - 127.0.0.1
      - 10.0.0.0/8

  # Example HTTPS handler with a custom trusted CA bundle.
  # - path: /internal/
  #   handler: https://api.internal.example.com/v1/
  #   trusted_ca:
  #     name: internal-api
  #     cert_path: /etc/pki/ca-trust/source/anchors/internal-api.crt

  # Example WebSocket handler over TLS.
  # - path: /websocket/
  #   handler: wss://server.eu.example.com:9000/websocket/
  #   trusted_ca:
  #     name: server-ws
  #     cert_path: /etc/pki/ca-trust/source/anchors/server-ws.crt

  # Example permanent redirect route.
  # Exactly one of handler or redirect must be set per route.
  # - path: /legacy/
  #   redirect: https://www.example.com/new-home/

  # Fallback for everything else.
  - path: /
    handler: http://127.0.0.1:3000
